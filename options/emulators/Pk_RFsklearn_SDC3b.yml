# general settings
name: Pk_SDC3b_RF
num_gpu: auto  # automatic GPU detection
manual_seed: !!int 10

model_type: RandomForestModel
# network architecture
network_opt:
  n_estimators: !!int 500
  max_depth: !!int 30
  min_samples_split: !!int 2
  min_samples_leaf: !!int 1
  n_jobs: !!int -1  # use all CPU cores

# training settings
train:
  total_iter: !!int 20000
  ema_decay: !!float 0.999
  optimizer_opt:
    type: Adam
    lr: !!float 0.001
    weight_decay: !!float 0.0001 #0.0001
  scheduler:
    type: MultiStepLR
    milestones: [5000, 7500]
    gamma: !!float 0.5

# dataset options
dataset:
  type: BaseDataset
  batch_size_per_gpu: !!int 20000
  num_worker_per_gpu: !!int 4
  params_opt:
    file: data/SDC3b/parameters_old.npy
    key: parameters
    names: ['zeta_eff', 'zeta_exp', 'rmfp', 'Vc']
    log: []
    discard: []
    normalization: norm_minmax
  targets_opt:
    file: data/SDC3b/power_spectra_old.npy
    offset: !!float 0.0
    log: !!bool false
  data_dims:
    z:
      file: data/SDC3b/z_power_spectra.npy
      log: !!bool false
      lims_nsample: [!!float nan, !!float nan, !!float nan] # [6.535, 7.962, !!float nan]
    kperp:
      file: data/SDC3b/bins_kper.txt
      log: !!bool false
      lims_nsample: [!!float nan, !!float nan, !!float nan] # [!!float 5e-2, !!float 5e-1, !!float nan]
    kpar:
      file: data/SDC3b/bins_kpar.txt
      log: !!bool false
      lims_nsample: [!!float nan, !!float nan, !!float nan] # [!!float 5e-2, !!float 5e-1, !!float nan]

path:
  pretrain_network_g: ~
  strict_load_g: true
  resume_state: ~

# validation settings
val:
  val_freq: !!float 1000

logger:
  print_freq: !!int 200
  save_checkpoint_freq: !!float 1000
  use_tb_logger: !!bool true
  wandb:
    project: ~
    resume_id: ~

dist_params:
  backend: nccl
  port: !!int 29500
